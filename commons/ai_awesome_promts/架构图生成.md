请帮我创建一个详细的系统架构图，需要包含以下内容：

1. 系统分层结构：
   - 前端层（如 Vue.js/React 应用）
   - 后端层（如 Node.js/Java 应用）
   - 基础设施层（如数据库、消息队列等）

2. 每个层级需要包含：
   - 主要组件
   - 子模块
   - 具体功能点
   - 关键方法/接口

3. 组件关系：
   - 组件间的调用关系
   - 数据流向
   - 依赖关系
   - 外部系统集成

4. 格式要求：
   - 使用 D2 格式
   - 方向设置为 right
   - 使用嵌套结构表示层级关系
   - 使用不同颜色区分不同层级
   - 为每个组件添加详细的中文说明

5. 样式规范：
   - 顶层容器使用浅灰色背景 (#f0f0f0)
   - 二级容器使用中灰色背景 (#e0e0e0)
   - 三级容器使用深灰色背景 (#d0d0d0)
   - 使用矩形形状
   - 标签使用中文，包含功能说明

6. 连接关系：
   - 使用箭头表示调用关系
   - 清晰标注数据流向
   - 显示组件间的依赖关系

7. 特别说明：
   - 需要包含完整的系统组件
   - 每个组件都要有详细的功能说明
   - 保持代码结构清晰易读
   - 使用注释说明重要部分

请按照以上要求，为我生成一个完整的系统架构图。
